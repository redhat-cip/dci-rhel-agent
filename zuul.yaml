---
- project:
    name: dci-rhel-agent
    vars:
      registry: quay.io
      tag: "distributedci/dci-rhel-agent"
      version: latest-7
    check:
      jobs:
        - dci-build-container-job
        - dci-rpm-build
    gate:
      jobs:
        - dci-build-container-job
        - dci-rpm-build
    dci-post:
      jobs:
        - dci-rpm-build
        - dci-build-and-push-container-job
        - dci-rpm-publish:
            dependencies:
              - dci-rpm-build
...
