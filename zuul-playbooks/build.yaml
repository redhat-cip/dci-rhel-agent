- hosts: testrunner
  tasks:
    - name: Start docker daemon
      service:
        name: docker
        state: started
      become: true

    - name: "Build {{ tag }} image"
      shell: "docker build --tag {{ tag }} --file ./Dockerfile ."
      become: true
      args:
        chdir: "{{ zuul.project.src_dir }}"